services:
  heimdall:
    image: linuxserver/heimdall
    container_name: heimdall
    volumes:
      - ${PATH}config:/config
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Chicago
    ports:
      - 8205:80
      - 8206:443
    restart: unless-stopped
